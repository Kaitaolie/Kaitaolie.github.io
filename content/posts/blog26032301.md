---
title: "基于 WSL 在 VS Code 上配置 C/C++ 开发环境"
date: 2026-03-23T00:00:00Z
draft: false
categories: ["技术"]
tags: ["WSL", "C++", "环境搭建"]
slug: "26032301"
---

> **摘要**：在 Windows 环境下，通过 WSL (Windows Subsystem for Linux) 配合 VS Code，可以获得接近原生的 Linux 开发体验。本文记录如何在 WSL 中配置高效的 C/C++ 开发环境。

---

### 前言

**WSL** 是 **Windows Subsystem for Linux**（Windows 的 Linux 子系统）的缩写，是 Windows 上的一项原生功能。通过该功能，你可以实现在 Windows 系统上运行 Linux 环境而不需要开启虚拟机，可谓十分方便。

鉴于 Linux 环境下进行 C/C++ 开发极其友好，本文主要探讨通过 **VS Code + WSL** 配置 C/C++ 开发环境的可行性。

---

### 为什么选择 WSL？

相较于在 Windows 上使用 MinGW 进行编译开发，基于 WSL 的开发环境主要有以下优点：

* **配置过程更简洁**：
    MinGW 方案在配置过程中通常需要手动处理复杂的系统环境变量，且正确下载并安装 MinGW 本身（确保不报错）就是一项挑战。而 WSL 可以通过在 CMD 或 PowerShell 中敲几行命令实现。
    
* **编译支持更完整**：
    根据 VS Code 官方文档，自 2024 年 11 月 3 日起，MSYS2 已默认**禁用 mingw-w64 的通配符支持**。这直接影响了构建命令中对 `*.cpp` 等通配符的处理方式。
    
    > *“Starting November 3, 2024, MSYS2 has disabled wildcard support for mingw-w64 by default. This affects how wildcards such as '\*.cpp' are handled in build commands.”*  
    >
    而 WSL 由于内置 GCC 编译器仍然支持对诸如`g++ *.cpp -o main`的命令，这使得其在编译多文件时更为方便。

### WSL 的配置

参阅 WSL 的官方文档，我们开始在 Windows 上配置 WSL。

#### 配置前的准备

接下来我来演示从零开始安装 WSL。

##### 1. 检查设备是否开启虚拟化

在 CMD 或者 PoweShell 中输入这个命令检查设备是否开启了 CPU 虚拟化：
```PowerShell
Get-ComputerInfo -Property "HyperV*"
```
输入之后输出结果中
`HyperVRequirementVirtualizationFirmwareEnabled`
返回值应该是`Ture`(如下图所示)
![PowerShell输出结果](/images/26032301/2026-03-23_21-06-06.png)

##### 2.系统准备工作

在 PowerShell里面执行以下命令：
```PowerShell
# 开启 WSL
dism.exe /online /enable-feature /featurename:Microsoft-Windows-Subsystem-Linux /all /norestart

# 开启虚拟机平台
dism.exe /online /enable-feature /featurename:VirtualMachinePlatform /all /norestart
```
之后系统会要求你重启，或者你可以手动重启。

除此之外还可以通过**控制面板**来开启这两个功能：
在 Windows 搜索里面搜索“控制面板”并打开

![搜索](/images/26032301/2026-03-23_21-25-18.png)

![搜索](/images/26032301/2026-03-23_21-25-50.png)

![搜索](/images/26032301/2026-03-23_21-26-59.png)

然后在控制面板中寻找并打开“启用或关闭Windows功能”(如图所示)

![启用或关闭Windows功能](/images/26032301/2026-03-23_21-27-30.png)

![启用或关闭Windows功能](/images/26032301/2026-03-23_21-28-50.png)

将 Hyper-v 、Windows 的 Linux 子系统、虚拟机平台全部勾选上点击确定，

![启用或关闭Windows功能](/images/26032301/2026-03-23_21-30-10.png)

之后系统会要求重启，至此准备工作全部完成。

#### 安装 WSL 

在前面的准备工作完成之后，接下来的 WSL 安装极为简单。
在 CMD 或者 PowerShell 中执行以下命令：

```PowerShell
wsl --install
```

![cmd命令输出](/images/26032301/2026-03-23_18-35-30.png)

在**科学上网工具**的帮助下通常几秒就可以完成下载并开始安装

![cmd命令输出](/images/26032301/2026-03-23_18-56-50.png)

官方文档中表示执行命令后会默认下载 Ubuntu 系统（然鹅我在实践过程中并非如此），那么在安装完成之后执行`wsl -l -v`可以查看已安装的 Linux 发行版。

![cmd命令输出](/images/26032301/2026-03-23_22-01-34.png)

之后弹出的命令框会要求你设置用户名和命名，**注意**：在设置密码时密码不会显示！

安装好 WSL 之后我们在 Window 终端中执行`wsl`就可以转换到 Linux 环境，路径提示符会从类似`C:\Users\Name`变成`user@hostname:/mnt/c/Users/Name`这样的。我们在终端中执行以下命令来检查更新：

```bash
sudo apt update && sudo apt upgrade
```

输入密码之后就开始跑码了

[终端输出](/images/26032301/2026-03-23_22-46-49.png)

到这里 WSL 安装工作实际上已经完成了，但是为了后续工作我们还需要进一步设置（如果 Linux 发行版是 Ubuntu 的话是可以直接跳到下一步的）

#### 进一步的工作

这里给出一些 WSL 常用命令

```PowerShell
# 安装默认的 Ubuntu 发行版（需重启后生效）
wsl --install

# 在当前目录打开 WSL
wsl

# 查看已安装发行版及其运行版本 (WSL 1/2)，简写为`wsl -l -v`
wsl --list --verbose

# 删除 WSL 所有数据（危险操作）
wsl --unregister <发行版名>

# 列出可下载的 Linux 发行版
wsl --list --online

# 下载特定 Linux 发行版
wsl --install -d <发行版名>
```

这是执行`wsl --list --online`的输出

![cmd命令输出](/images/26032301/2026-03-23_22-29-21.png)

接下来我们在 Linux 环境检查 C/C++ 编译链：
我们在终端中执行以下命令

```bash
# 检查 GCC 编译器版本
gcc --version

# 检查 G++ 编译器版本
g++ --version

# 检查 GDB 调试器版本
gdb --version
```

输出结果如下图则说明编译链完整

![终端输出](/images/26032301/2026-03-23_22-50-09.png)

如果输出结果显示`command not found`说明你需要安装工具链，那么请执行以下命令

```bash
# 首先更新软件源索引
sudo apt update

# 安装 build-essential 工具包（包含 gcc, g++, make 等）
sudo apt install build-essential gdb
```

然后再次检查即可，至此我们在 WSL 的配置工作就全部完成了

### VS Code 的安装